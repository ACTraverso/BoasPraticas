{
  "name": "boaspraticas",
  "version": "1.0.0",
  "description": "BoasPracticasCypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run": "npx cypress run --browser=chrome --headless --spec 'cypress/e2e/*.cy.js' --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ACTraverso/BoasPraticas.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ACTraverso/BoasPraticas/issues"
  },
  "homepage": "https://github.com/ACTraverso/BoasPraticas#readme",
  "dependencies": {
    "cypress": "^14.0.0"
  },
  "devDependencies": {
    "mocha-allure-reporter": "^1.4.0"
  }
}
